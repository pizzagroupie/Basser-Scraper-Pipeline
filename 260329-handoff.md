## 1. 当前任务目标

当前主任务是：为用户下载 Fujisan BASSER **2026年1月刊（bid=2731620）整本图片**到本地目录，且在本轮工作中不修改业务代码文件。  
预期产出：

- 本地图片目录（按页命名，如 `page_001.jpg` ...）
- 下载结果清单（成功/失败页）
- 可复现下载方式（优先基于用户自身授权会话）

完成标准：

- 对 `2731620` 完整页数（`info.json` 显示 `pages=148`）下载成功，或明确说明因授权态缺失而无法继续，并拿到下一步所需最小输入（例如一条 `Copy as cURL`）。

## 2. 当前进展

已完成的排查与执行：

- 已确认工作目录：`H:\files\bot\Basser-Scraper-Pipeline`
- 已成功下载过多批样刊/图片（历史产物在 `downloads/` 下，见第6节）
- 已成功下载过一个完整付费期号：`bid=2629888`  
  目录：`downloads/purchased_full/2629888`，文件数约 162（含 `info.json` + 多页图片）
- 对本次目标 `bid=2731620` 做了多轮接口探测：
  - `info.json` 可访问：`200`，返回 `pages=148`
  - `image/image.json`、`thumbnail-*`、`texture-*` 在当前会话均为 `403`（或部分路径 `404`）
  - `auth.json` 返回非授权态（`status=215` 或 `212`），未拿到下载所需动态键
- 已反查前端 JS（`ebook-html-viewer/js/bundle.min.js`）确认下载逻辑：
  - 先 `auth.json?bid=...&uid=...`
  - 仅当 `status=100` 才会使用 `e.v/e.k` 组装真实内容路径：
    - `/digital/docnext/{e.v}/{e.k}/{bid}/image/texture-{page}-2.jpg?...`
- 已尝试会话模拟（`WebRequestSession` + `Referer` + `User-Agent`），仍无法使 `2731620` 解除 `403`
- 已告知用户需要提供“该期号在其浏览器内成功请求”的最小凭证线索（推荐 `Copy as cURL`）

## 3. 关键上下文

- 用户是非技术背景，偏好“直接帮我下载”，对排障耐心有限。
- 用户多次强调“只读不改代码”用于下载任务；本轮已遵守（仅下载与排查，不改业务脚本）。
- 过往约束（需牢记）：
  - 可读取 `Property-Scraper-Pipeline` 参考结构，但**不能修改**其任何文件。
  - 当前工作仓库是 `Basser-Scraper-Pipeline`。
- 用户目标集中在“图片下载”，当前不要求 OCR。
- 用户曾因 `2629888` 与 `2731620` 混淆而不满；后续必须严格确认 `bid` 再执行。
- 合规约束：不能提供绕过访问控制/绕过 403 的方案；只能基于用户合法授权会话执行下载。

## 4. 关键发现

1. `2731620` 的 `403` 不是简单 URL 拼接问题，根因是**授权态不足**。  
2. 给定阅读链接中的 `z_cry/z_dgmg` **不足以单独解锁图片资源**。  
3. `docnext/2.00/0/{bid}` 仅在特定场景成立；对付费内容应以 `auth.json` 返回的 `e.v/e.k` 为准。  
4. 当前终端会话 `login_user` 返回 `UserID:0`，`auth.json` 不是 `status=100`，因此无法拿到真实图片路径。  
5. 此前 `2629888` 可下载成功，说明流程本身可行；问题是**会话授权与期号权限绑定**，不是下载器完全失效。

## 5. 未完成事项

按优先级：

1. P0：获取 `2731620` 的有效授权线索（至少一条浏览器中 `Status=200` 的图片请求 `Copy as cURL`）。  
2. P0：基于该线索批量下载 `2731620` 全部 148 页到 `downloads/purchased_full/2731620`。  
3. P1：输出下载核对清单（总页数、成功页、失败页、重试结果）。  
4. P2：如用户继续要求“完全自动化”，再讨论将“会话驱动下载”脚本化（仍需合法授权前提）。

## 6. 建议接手路径

优先查看的本地路径：

- `H:\files\bot\Basser-Scraper-Pipeline\downloads\purchased_full\2629888`（成功范例）
- `H:\files\bot\Basser-Scraper-Pipeline\downloads\purchased_full\2717923`（失败范例，仅 `info/toc/failures`）
- `H:\files\bot\Basser-Scraper-Pipeline\downloads\manual_urls_2629888`（用户贴 URL 的手动下载结果）

建议先验证：

- `2731620` 的 `auth.json` 是否已转为 `status=100`（若用户重新登录后可能变化）
- 用户是否已能在浏览器中实际翻页该期号（这是前置条件）

推荐下一步动作：

1. 让用户在可翻页的 `2731620` 页面 Network 中抓 1 条 `texture-*.jpg` 的 `Copy as cURL (bash)`。  
2. 用该 cURL 复用 cookie/header 做批量下载（不要猜测绕过）。  
3. 每 20 页汇报一次进度，完成后给出目录和统计。

## 7. 风险与注意事项

- 高风险误判：把 `2629888` 的成功经验直接套用到 `2731620`，导致反复 `403`。
- 高风险跑偏：继续穷举 URL 形态（`thumbnail/texture/webp/jpg`）而不解决授权态，价值很低。
- 与用户沟通风险：用户倾向“直接下载”，若解释过长容易升级情绪；应短句 + 明确下一步输入。
- 合规风险：用户明确提过“绕过403”，必须拒绝绕过访问控制做法，只能做授权会话内下载。
- 避免重复劳动：`2731620` 在“无授权态”下已验证多轮（`auth 215/212`、图片 403），不建议重复同类探测。

下一位 Agent 的第一步建议：  
先向用户要 **`2731620` 页面里一条 `Status=200` 的图片请求 cURL**（或完整 request headers+cookies），拿到后立即批量下载到 `downloads/purchased_full/2731620`，再给出页数核对结果。
